$!===========================================================
$! BUILD_M309_PLAN.COM - M309 saved-plan completion regression
$!===========================================================
$ SET NOON
$ SAVED_DEFAULT = F$ENVIRONMENT("DEFAULT")
$ PROCEDURE = F$ENVIRONMENT("PROCEDURE")
$ PROJECT_DIR = F$PARSE(PROCEDURE,,,"DEVICE") + -
F$PARSE(PROCEDURE,,,"DIRECTORY")
$ SET DEFAULT 'PROJECT_DIR'
$ FINAL_STATUS = 1
$
$ IF F$SEARCH("[]BUILD.DIR;*") .EQS. ""
$ THEN
$     CREATE/DIRECTORY [.BUILD]
$     IF .NOT. $STATUS THEN GOTO FAILED
$ ENDIF
$
$ CC/OBJECT=[.BUILD]M309_PLAN_TEST.OBJ -
/STANDARD=C99 -
/PREFIX_LIBRARY_ENTRIES=ALL_ENTRIES -
/INCLUDE_DIRECTORY=([.SRC]) -
[.SRC]M309_PLAN_TEST.C
$ IF .NOT. $STATUS THEN GOTO FAILED
$
$ LINK/EXECUTABLE=[.BUILD]M309_PLAN_TEST.EXE -
[.BUILD]M309_PLAN_TEST.OBJ
$ IF .NOT. $STATUS THEN GOTO FAILED
$
$ RUN [.BUILD]M309_PLAN_TEST
$ IF .NOT. $STATUS THEN GOTO FAILED
$
$ SET DEFAULT 'SAVED_DEFAULT'
$ EXIT 1
$
$ FAILED:
$ FINAL_STATUS = $STATUS
$ WRITE SYS$ERROR "M309 saved-plan completion/Markdown regression failed."
$ SET DEFAULT 'SAVED_DEFAULT'
$ EXIT 'FINAL_STATUS'
